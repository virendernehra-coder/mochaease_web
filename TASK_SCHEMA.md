# Current Task Schema Reference

This file contains the specific schemas and logic definitions for the task I am currently working on. 

## Active Task Schemas
(Paste task-specific schemas below)

create table public.business_info (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  business_currency text null,
  business_monthly_revenue_goal bigint null,
  business_id uuid null default gen_random_uuid (),
  user_id uuid null default gen_random_uuid (),
  updated_at timestamp with time zone null,
  constraint business_info_pkey primary key (id)
) TABLESPACE pg_default;