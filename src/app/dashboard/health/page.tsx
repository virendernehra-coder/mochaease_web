import React from 'react';
import HealthClient from './HealthClient';

export const metadata = {
    title: 'Business Health Dashboard | MochaEase',
    description: 'Monitor your enterprise health scorecard and AI recommendations.',
};

export default function HealthPage() {
    return <HealthClient />;
}
